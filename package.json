{
  "name": "@metorial-subspace/root",
  "private": true,
  "scripts": {
    "dev": "docker compose -p subspace -f ./docker-compose.dev.yml up --pull always --build slates-hub slates-registry signal function-bay forge voyager subspace-controller subspace-public subspace-worker subspace-connection-endpoint"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "prettier": "^3.5.3",
    "turbo": "^2.5.5",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "db",
    "clients/*",
    "provider-backends/*",
    "apps/*",
    "packages/*",
    "modules/*"
  ],
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}