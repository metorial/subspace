{
  "name": "@metorial-subspace/root",
  "private": true,
  "scripts": {
    "knip": "knip",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write .",
    "format": "prettier --write .",
    "dev": "docker compose -p subspace -f ./docker-compose.dev.yml up --pull always --build slates-hub slates-registry signal function-bay forge voyager shuttle subspace-controller subspace-public subspace-worker"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "prettier": "^3.5.3",
    "typescript": "5.8.2"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.2.15"
  },
  "packageManager": "bun@1.2.15",
  "workspaces": [
    "db",
    "clients/*",
    "provider-backends/*",
    "apps/*",
    "packages/*",
    "modules/*"
  ],
  "dependencies": {
    "knip": "^5.82.0"
  }
}